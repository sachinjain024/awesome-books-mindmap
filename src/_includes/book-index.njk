<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - Mind Map</title>
    <link rel="stylesheet" href="../shared/styles.css">
    <link rel="stylesheet" href="styles.css">
</head>
<body class="index-page">
    <div class="container">
        {% set hv = headerVariant or "option1" %}
        <header class="book-hero book-hero--{{ hv }}">
            {% if hv == "option1" %}
            <h1 class="book-title-main">{{ title }}</h1>
            <div class="book-meta-line book-meta-line--option1">
                <span class="book-tagline-inline">{{ subtitle }}</span>
                <span class="book-meta-dot">&bull;</span>
                <span class="book-author-inline">by {{ authors }}</span>
            </div>
            {% elif hv == "option2" %}
            <h1 class="book-title-main">{{ title }}</h1>
            <div class="book-hero-divider"></div>
            <div class="book-meta-line">
                <span class="book-tagline-inline">{{ subtitle }}</span>
                <span class="book-meta-dot">&bull;</span>
                <span class="book-author-inline">by {{ authors }}</span>
            </div>
            {% else %}
            <h1>{{ title }}</h1>
            <div class="subtitle">{{ subtitle }}</div>
            <div class="authors">by {{ authors }}</div>
            {% endif %}
        </header>

        <div class="intro">
            {{ content | safe }}
        </div>

        <blockquote class="philosophy">
            {{ epigraph.quote }}
            <cite>— {{ epigraph.author }}</cite>
        </blockquote>

        <div class="central-node">
            {{ centralNode.title }}<br/>
            <span style="font-size: 0.7em;">{{ centralNode.subtitle }}</span>
        </div>

        <div class="mindmap">
            {% for section in sections %}
            <div class="section">
                <div class="section-title">{{ section.title }}</div>
                <div class="chapters-grid">
                    {% for chapter in section.chapters %}
                    <a href="chapters/chapter-{{ '%02d' | format(chapter.number) }}.html" class="chapter-card">
                        <div class="chapter-number">Chapter {{ chapter.number }}</div>
                        <div class="chapter-name">{{ chapter.title }}</div>
                        {% if chapter.subtitle %}
                        <div class="chapter-subtitle">{{ chapter.subtitle }}</div>
                        {% endif %}
                        <div class="chapter-description">{{ chapter.description }}</div>
                    </a>
                    {% endfor %}
                </div>
            </div>
            {% endfor %}
        </div>

        <div class="key-teachings">
            <h3>{{ keyTeachingsTitle or "Core Stoic Principles" }}</h3>
            <ul>
                {% for teaching in keyTeachings %}
                <li><strong>{{ teaching.principle }}</strong> {{ teaching.description }}</li>
                {% endfor %}
            </ul>
        </div>

        {% if quickWins and quickWins.items and quickWins.items.length %}
        <div class="quick-wins">
            <h3>{{ quickWins.title or "Quick Wins" }}</h3>
            <ul>
                {% for item in quickWins.items %}
                <li>{{ item }}</li>
                {% endfor %}
            </ul>
        </div>
        {% endif %}
    </div>

    <script src="../shared/highlight.js" defer></script>
</body>
</html>
